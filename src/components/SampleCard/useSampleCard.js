import React from 'react';
const useSampleCard = (props) => {
  const data = {};
  const handleButtonClick = () => {};

  const fns = { handleButtonClick };

  return { data, fns };
};

export default useSampleCard;
